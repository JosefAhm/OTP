# Example environment variables for the otp project
# Copy this file to .env.local (or .env) and replace the placeholders with real values.
# Do NOT commit .env.local with real secrets to version control.

# Public URL for your Supabase project (used in client code)
NEXT_PUBLIC_SUPABASE_URL=""

# Service role key for Supabase (server-side only). Keep this secret.
SUPABASE_SERVICE_ROLE_KEY=""

# Optional: if you use a database URL or other secrets, add them here
# DATABASE_URL="postgresql://user:pass@host:5432/dbname"

# Optional: set Node environment (development|production)
# NODE_ENV=development
